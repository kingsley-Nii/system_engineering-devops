#!/usr/bin/env bash
# Kills the 4-to_infinity_and_beyond process using the kill command

# Find the PID of the running script (using the exact filename)
PID=$(pgrep -f "4-to_infinity_and_beyond")

# Send the default SIGTERM signal (signal 15)
if [ -n "$PID" ]; then
    kill "$PID"
fi
