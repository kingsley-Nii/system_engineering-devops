#!/usr/bin/env bash
# Kills the 7-highlander process using the SIGKILL signal (signal 9)

# Find the PID of the running script (using the exact filename)
PID=$(pgrep -f "7-highlander")

# Send the SIGKILL signal (signal 9)
if [ -n "$PID" ]; then
    kill -9 "$PID"
fi
